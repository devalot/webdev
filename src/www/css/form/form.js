/**
 * Exercise 1:
 *
 * Validate a license plate number.
 *
 * See the validation rules in the CSS file.
 *
 * @param {String} plate The license plate number.
 * @return {Bool} True if the plate is valid.
 */
function validatePlateNumber(plate) {
  return true;
}

/**
 * Exercise 2:
 *
 *   1. Listen for input events on the license plate field and
 *      validate them with the `validatePlateNumber' function.
 *
 *   2. Listen for form submissions and prevent the submission if the
 *      license plate number is not valid.
 */
