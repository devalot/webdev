// TODO: create websocket server with `ws` library

// TODO: when a websocket connection occurs:
//   When a 'pong' is received, mark the connection as still alive

// TODO At a regular interval:
//   If any clients are "dead" terminate them with `#terminate`
//   Mark each client as dead and ping them, to see if they pong back
