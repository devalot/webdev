### Asynchronous HTTP: `XMLHttpRequest`

  * Process Overview

    #. Create an `XMLHttpRequest` object
    #. Listen for `load` and `error` events
    #. Open the request
    #. Send the request

  * Complete Example

    ~~~ {.javascript insert="../../src/examples/js/xhr.js"}
    ~~~
